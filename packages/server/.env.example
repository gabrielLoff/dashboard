# External API keys — fill these in to switch from mock to live data
OPENWEATHER_API_KEY=
NEWSLETTER_API_KEY=
CALENDAR_API_KEY=
FREEGAMES_API_KEY=

# Server port
PORT=3001

# Set to "true" to use mock data, "false" to call real APIs
MOCK=true

# Cache TTLs in seconds
CACHE_TTL_WEATHER=600
CACHE_TTL_NEWS=1800
CACHE_TTL_AGENDA=600
CACHE_TTL_GAMES=7200
